scroll_page_fractional:
  ページの指定された割合分だけ画面をスクロールします。

  正の値は下方向にスクロールし、負の値は上方向にスクロールします。

  例えば、`scroll_page_fractional:0.5` はページの半分だけ下方向に
  スクロールし、`scroll_page_fractional:-1.5` は1ページ半だけ
  上方向にスクロールします。
