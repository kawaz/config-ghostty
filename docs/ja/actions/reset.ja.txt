reset:
  ターミナルをリセットします。

  実行中のプログラムがターミナルを破損した状態に陥った場合、
  これを使用して多くの問題を修正できます。`reset` コマンドを実行
  するのと同等です。

  vim などの TUI プログラム内でこれを実行した場合、
  プログラムが破損する可能性があります。シェル内で実行した場合、
  新しいプロンプトを取得するために後で Enter を押す必要があります。
