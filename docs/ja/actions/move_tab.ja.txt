move_tab:
  タブを相対的なオフセット分だけ移動します。

  正の値はタブを前方に移動し、負の値は後方に移動します。新しい位置がタブ
  リストの範囲外の場合、リスト内でループして巻き込みが行われます。

  例えば、`move_tab:1` はタブを1つ前の位置に移動し、既に最後のタブだった
  場合は巻き込んで最初のタブになります。同様に、`move_tab:-1` はタブを1つ
  後ろの位置に移動し、最初のタブだった場合は最後のタブになります。
