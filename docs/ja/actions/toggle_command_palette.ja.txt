toggle_command_palette:
  コマンドパレット (command palette) を切り替える。

  コマンドパレットはポップアップで、実行可能なアクションを表示し、
  関連するキーバインディング（存在する場合）、アクションをフィルタリング
  する検索バー、そしてアクションを実行する機能を提供します。

  Linux でこれを使用するには libadwaita 1.5 以降が必要です。
  現在の libadwaita バージョンは `ghostty +version` を実行して確認できます。
