# 選択したテキストをクリップボードに自動的にコピーするかどうか。`true`
# の場合、選択クリップボードにコピーすることを優先します。そうでない場合、
# システムクリップボードにコピーします。
#
# 値 `clipboard` は常にテキストを選択クリップボードとシステムクリップボード
# の両方にコピーします。
#
# 中クリックペーストは常に選択クリップボードを使用します。中クリック
# ペーストは、これが `false` でも常に有効です。
#
# デフォルト値は Linux と macOS では true です。
copy-on-select = true
