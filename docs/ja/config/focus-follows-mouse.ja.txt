# true の場合、複数の分割ペイン（split panes）があるとき、マウスが
# フォーカスされたペインを選択します。これは現在フォーカスされたウィンドウ
# にのみ適用されます。例えば、フォーカスされていないウィンドウ内の分割に
# マウスを移動しても、その分割はフォーカスされず、ウィンドウも手前に移動
# しません。
#
# デフォルト値は false です。
focus-follows-mouse = false
