# macOS アプリアイコンの画面の色。
#
# スクリーンはリニアグラデーションなため、グラデーションを構成する複数の色を指定できます。
# 最大 64 個のカンマ区切りの色を 16 進数（#RRGGBB または RRGGBB）または名前付き X11 色として
# 指定できます。最初の色はグラデーションの下で、最後の色はグラデーションの上です。
#
# 注: `macos-icon` が `custom-style` に設定されている場合、この設定が必要です。
macos-icon-screen-color =
