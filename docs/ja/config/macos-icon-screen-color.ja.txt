# macOS アプリアイコンのスクリーン部分の色
#
# スクリーンは線形グラデーション（linear gradient）なので、複数の色を指定して
# グラデーションを構成することができます。最大64個のカンマ区切り色を16進数
# (`#RRGGBB` または `RRGGBB`) または X11 名前付き色として指定できます。
# 最初の色がグラデーションの下部で、最後の色がグラデーションの上部です。
#
# 注記: この設定は `macos-icon` が `custom-style` に設定されている場合に必須です。
macos-icon-screen-color =
