# 読み込むカスタムCSSファイル。
#
# GTK CSSのドキュメントは以下のリンクで見つけることができます：
#
#   * https://docs.gtk.org/gtk4/css-overview.html - GTK CSSの概要
#   * https://docs.gtk.org/gtk4/css-properties.html - サポートされている
#     CSSプロパティの包括的なリスト
#
# `env GTK_DEBUG=interactive ghostty` でGhosttyを起動して、GTK Inspectorを
# 使ってリアルタイムでGhosttyのCSSを調整してください。CSSファイルのエラーは、
# Ghosttyを起動したターミナルで報告されます。GTK Inspectorの詳細については、
# https://developer.gnome.org/documentation/tools/inspector.html を参照してください。
#
# この設定は複数回繰り返して、複数のファイルを読み込むことができます。
# ファイルパスの先頭に ? 文字を付けると、ファイルが存在しない場合のエラーを
# 抑制できます。リテラルな ? 文字で始まるファイルを含めたい場合は、ファイル
# パスをダブルクォーテーション (") で囲んでください。
# 単一のスタイルシートのファイルサイズ上限は5MiBです。
#
# 利用可能: 1.1.0以降
gtk-custom-css =
