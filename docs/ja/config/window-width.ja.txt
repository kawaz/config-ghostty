window-width = 0
# ウィンドウの開始位置。この位置はピクセル単位で、プライマリ
# モニターの左上隅を基準とします。効果を発揮するには両方の値を設定する必要があります。
# 値が1つだけ設定されている場合は、無視されます。
#
# ウィンドウマネージャーがポジションに制限を課したり、位置をオーバーライドする
# 可能性があることに注意してください。例えば、タイリングウィンドウマネージャーは
# グリッド内に適合させるためにウィンドウを特定の位置に強制する可能性があります。
# これについてGhosttyができることはありませんが、最善の努力をします。
#
# また、負の値もオペレーティングシステムとウィンドウマネージャーに依存します。
# 一部のウィンドウマネージャーは、ウィンドウがオフスクリーンに配置されることを
# 許可しない可能性があります。
#
# 無効な位置はランタイム固有ですが、一般的に位置は最も近い有効な位置に
# クランプされます。
#
# macOSでは、ウィンドウの位置は表示画面領域の左上隅を基準とします。
# つまり、メニューバーが表示されている場合、ウィンドウはメニューバーの
# 下に配置されます。
#
# 注: これはmacOSでのみサポートされています。GTKランタイムはウィンドウの位置設定を
# サポートしていません。ウィンドウはX11でのみ位置を設定でき、Waylandではできません。
window-position-x =
