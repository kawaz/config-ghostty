# 選択範囲の前景色および背景色。設定しない場合、選択範囲の色はウィンドウの
# 背景色と前景色を反転したものになります
# （セルの背景色/前景色と混同しないようご注意ください）。
# 16進数（`#RRGGBB` または `RRGGBB`）または名前付きX11カラーで指定します。
# バージョン 1.2.0 以降、これを `cell-foreground` に設定してセルの前景色に
# 合わせることも、`cell-background` に設定してセルの背景色に合わせることも
# できます。
selection-foreground =

