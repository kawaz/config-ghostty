# テキスト選択の背景色と前景色。設定されていない場合、選択色は単なる
# 反転したウィンドウの背景と前景です（注意: セル bg/fg と混同しないでください）。
# 16 進数（`#RRGGBB` または `RRGGBB`）または名前付き X11 色として指定されます。
# バージョン 1.2.0 以降、セル前景色に一致するように `cell-foreground` に設定
# することもできます。またはセル背景色に一致するように `cell-background` に
# 設定することもできます。
selection-foreground =
selection-background =
