# `font-thicken` が有効な場合のフォント太くする強度。
#
# 有効な値は `0` から `255` の間の整数です。`0` は太くしない状態に
# *対応していません* 。むしろ、利用可能な最も軽い太くする強度に対応しています。
#
# `font-thicken` が `false` に設定されている場合は効果がありません。
#
# 現在のところ、macOS でのみサポートされています。
font-thicken-strength = 255
