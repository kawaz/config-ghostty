# `font-thicken` が有効な場合の太字化の強度。
#
# 有効な値は 0 から 255 の間の整数です。`0` は太字化なしに対応していません。
# むしろ、利用可能な最も明るい太字化に対応しています。
#
# `font-thicken` が `false` に設定されている場合は効果がありません。
#
# これは現在のところ macOS でのみサポートされています。
font-thicken-strength = 255
