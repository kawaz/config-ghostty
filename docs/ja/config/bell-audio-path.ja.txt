# `audio` がベル機能として有効な場合、これはオーディオファイルへのパスです。
# パスが絶対パスでない場合は、参照元の設定ファイルのディレクトリからの相対パス、
# または CLIフラグとして使用される場合は現在の作業ディレクトリからの相対パスと見なされます。
# パスの先頭に `~/` を付けてユーザーのホームディレクトリを参照することができます。
# (GTK のみ)
#
# 利用可能なバージョン: 1.2.0
bell-audio-path =
