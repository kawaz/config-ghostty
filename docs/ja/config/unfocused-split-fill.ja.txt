# フォーカスされていないスプリット (split) を薄暗くする色。フォーカスされていないスプリットは、
# スプリット上に半透明の矩形を描画することで薄暗くなります。この設定はその矩形の色を設定し、
# 薄暗くする効果を細かく制御するために使用できます。
#
# デフォルトはバックグラウンドカラーになります。
#
# 16進数 (`#RRGGBB` または `RRGGBB`) または名前付きX11カラーとして指定します。
unfocused-split-fill =
