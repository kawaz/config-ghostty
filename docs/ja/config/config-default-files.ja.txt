# これが true の場合、デフォルト設定ファイル パスがロードされる。
# デフォルト設定ファイル パスは現在のところ XDG config path（$XDG_CONFIG_HOME/ghostty/config）のみである。
#
# これが false の場合、デフォルト設定パスはロードされない。
# これは、外部効果を最小化する方法で CLI から Ghostty を使用することを
# 直接対象としている。
#
# これは CLI のみの設定である。これを設定ファイルに設定しても効果はない。
# エラーではないが、何もしない。この設定は CLI 引数を介してのみ設定できる。
config-default-files = true
