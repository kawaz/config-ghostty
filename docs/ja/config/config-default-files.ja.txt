# これが true の場合、デフォルトの設定ファイルパスが読み込まれます。
# デフォルトの設定ファイルパスは現在のところ、XDG
# config パス（$XDG_CONFIG_HOME/ghostty/config）のみです。
#
# これが false の場合、デフォルトの設定パスは読み込まれません。
# これは、外部の影響を最小限に抑えた方法で CLI から Ghostty を使用する
# ことを目的としています。
#
# これは CLI のみの設定です。設定ファイルで設定した場合は
# 効果がありません。これはエラーではありませんが、何もしません。
# この設定は CLI 引数を使用してのみ設定できます。
config-default-files = true
