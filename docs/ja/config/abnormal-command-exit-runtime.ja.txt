# プロセスの終了を異常と判断する実行時間（ミリ秒）の閾値
# プロセスが過度に早く終了した場合にエラーメッセージを表示するために使用されます。
#
# Linuxでは、この設定はゼロ以外の終了コードとペアで使用する必要があります。macOSでは、
# ログインコマンド経由でシェルプロセスが起動される方法の関係上、任意の終了コードを許可します。
abnormal-command-exit-runtime = 250
