# ターミナル カラーを解釈するときに使用するカラー スペース。
# 「ターミナル カラー」は、設定で指定されたカラー、および直接カラー SGR
# シーケンスで生成されたカラーを指します。
#
# 有効な値：
#
#   * `srgb` - sRGB カラー スペースでカラーを解釈します。これはデフォルトです。
#   * `display-p3` - Display P3 カラー スペースでカラーを解釈します。
#
# この設定は現在、macOS でのみサポートされています。
window-colorspace = srgb
