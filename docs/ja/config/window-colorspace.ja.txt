# ターミナルの色を解釈する際に使用するカラースペース。「ターミナル色」
# は、設定で指定された色と直接カラーSGRシーケンスで生成された色を
# 指します。
#
# 有効な値:
#
#   * `srgb` - 色をsRGBカラースペースで解釈します。これがデフォルトです。
#   * `display-p3` - 色をDisplay P3カラースペースで解釈します。
#
# この設定は現在macOSでのみサポートされています。
window-colorspace = srgb
