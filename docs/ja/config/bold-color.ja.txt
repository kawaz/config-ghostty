# ターミナルの太字テキストに使用される色を変更します。
#
# 特定の色に設定できます。形式は `background` または `foreground` と同じもの
# が使用できます（例：`#RRGGBB` ですが、その他の形式も対応しています。詳細は
# 上記のドキュメントを参照してください）。特定の色が設定されている場合、この
# 色は常にデフォルトの太字テキスト色に使用されます。残りの太字色を
# `bright` に設定します。
#
# これを `bright` に設定することもできます。これにより太字テキストに明るい
# 色パレットが使用されます。例えば、テキストが赤い場合、太字は明るい赤色
# を使用します。ターミナルパレットは `palette` で設定されていますが、OSC 4
# などのエスケープシーケンスを使用してターミナルアプリケーション自体で上書き
# することもできます。（Ghostty 1.2.0 以降、以前の設定である
# `bold-is-bright` は廃止され、この使用方法に置き換えられました）。
#
# Ghostty 1.2.0 以降で利用可能です。
bold-color =
