# これらの設定は、フォントによって決定される様々なメトリクスを調整する動作です。
# 値は整数（1、-1など）またはパーセンテージ（20%、-15%など）にすることができます。
# いずれの場合でも、値は元の値を変更する量を表します。
#
# 例えば、値が `1` の場合は値を1増加させます。リテラルに1に設定するのではありません。
# 値が `20%` の場合は値を20%増加させます。以下同様です。
#
# これらの値の検証はほとんど、またはまったく行われないため、不正な値
# （例：`-100%`）によってターミナルが使用不可能になる可能性があります。
# 注意深く、合理的に使用してください。
#
# いくつかの値は最小値または最大値にクランプされます。これにより、特定の値が
# 無視されているように見える場合があります。例えば、多くの `*-thickness` 調整は
# 1px以下にはできません。
#
# `adjust-cell-height` には説明すべき追加の動作があります：
#
#   * フォントがセル内で垂直方向に中央に配置されます。
#
#   * カーソルはフォントと同じサイズを保持しますが、
#     `adjust-cursor-height` で別々に調整することができます。
#
#   * Powerlineグリフはセルの高さに合わせて調整されるため、
#     ステータスラインのような見た目が揃ったままになります。
adjust-cell-width =
