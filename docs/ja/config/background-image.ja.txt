# ターミナルの背景画像。
#
# これはPNGまたはJPEGファイルへのパスである必要があります。その他の画像形式は
# まだサポートされていません。
#
# 背景画像は現在、ウィンドウごとではなくターミナルごとです。ウィンドウを
# 頻繁に分割する場合、背景画像は分割全体で繰り返されます。Ghosttyの将来の改善で
# これに対応する予定です。
#
# 警告: 背景画像は現在、ターミナルごとにVRAMで複製されています。
# 十分に大きい画像の場合、これはメモリ使用量（特にVRAM使用量）の大幅な増加に
# つながる可能性があります。将来のGhosttyの改善により、これは画像テクスチャを
# ターミナル間で共有することで解決されます。
#
# 利用可能: 1.2.0以降
background-image =
