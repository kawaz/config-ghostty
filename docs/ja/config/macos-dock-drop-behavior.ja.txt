# macOS ドックの Ghostty アイコンにファイルまたはフォルダをドロップしたときの
# ウィンドウの動作を制御する。
#
# 有効な値：
#
#   * `new-tab` - 現在のウィンドウに新しいタブを作成するか、存在しない場合は
#     新しいウィンドウを開く。
#   * `window` - 無条件に新しいウィンドウを作成。
#
# デフォルト値は `new-tab`。
#
# この設定は macOS でのみサポートされており、他のプラットフォームでは効果がない。
macos-dock-drop-behavior = new-tab
