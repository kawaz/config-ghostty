# カーソルのデフォルトのブリンク状態を設定します。これはデフォルト状態です。
# 実行中のプログラムは `DECSCUSR` (`CSI q`) を使用してカーソルスタイルを
# オーバーライドできます。
#
# これが設定されていない場合、カーソルはデフォルトでブリンクします。
# これは下記に記載されているように "true" 値と同じではないことに注意してください。
#
# これがまったく設定されていない場合（`null`）、Ghostty は DEC Mode 12
# （AT&T カーソルブリンク）をブリンクのオン/オフを切り替える別のアプローチとして
# サポートします。これが null 以外の値に設定されている場合、DEC Mode 12 は
# 無視されますが、`DECSCUSR` は引き続きサポートされます。
#
# 有効な値は：
#
#   * ` ` （空白）
#   * `true`
#   * `false`
cursor-style-blink =
