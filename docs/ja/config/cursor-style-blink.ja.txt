# カーソルのデフォルトの点滅状態を設定します。これはあくまでデフォルト
# 状態です。実行中のプログラムは `DECSCUSR` (`CSI q`) を使用してカーソル
# スタイルをオーバーライドできます。
#
# この設定が行われていない場合、カーソルはデフォルトで点滅します。これは以下で
# 説明する「true」値と同じではないことに注意してください。
#
# この設定が全く行われていない場合（`null`）、Ghostty は DEC Mode 12
# (AT&T カーソル点滅) を使用して、点滅を有効/無効にする別の方法を採用します。
# これが null 以外の値に設定されている場合、DEC モード 12 は無視されますが、
# `DECSCUSR` は引き続き尊重されます。
#
# 有効な値は以下の通りです：
#
#   * ` ` (空白)
#   * `true`
#   * `false`
cursor-style-blink =
