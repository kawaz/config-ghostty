# true の場合、セキュアインプット (secure input) が有効な時に Ghostty
# がグラフィカル表示を示します。このインジケーション (表示) は一般的に
# セキュアインプットが有効かどうかを知るために推奨されています。
#
# 通常、セキュアインプットはパスワードプロンプトが表示されている時
# のみアクティブになるか、手動で（通常は一時的に）有効にされます。
# ただし、セキュアインプットを常に有効にしている場合、このインジケ
# ーションが気になる可能性があり、無効にしたい場合があります。
macos-secure-input-indication = true
