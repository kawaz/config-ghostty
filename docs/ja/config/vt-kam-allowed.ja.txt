# true の場合、ターミナル内で "KAM" モード (ANSI モード 2) の使用を許可します。
# KAM はアプリケーションの要求に応じてキーボード入力を無効にします。これは一般的な
# 機能ではなく、有効にすることは推奨されません。KAM が必要であることを知っている
# 場合はそれが必要であることを知っています。そうでない場合は、それ以上のドキュメント
# は提供されません。必要かどうかが不明な場合は、必要ありません。
vt-kam-allowed = false
