# 開始ウィンドウの位置。この位置はピクセル単位で、プライマリ モニターの
# 左上隅を基準にしている。効果を持たせるには両方の値を設定する必要がある。
# 1 つの値だけが設定されている場合、それは無視される。
#
# ウィンドウマネージャーは位置に制限を設けたり、位置をオーバーライドしたり
# する可能性があることに注意してください。たとえば、タイリングウィンドウマネージャーは、
# グリッド内に適合するようにウィンドウを特定の位置にするように強制する可能性がある。
# Ghostty がこれについて何かできることはありませんが、努力するでしょう。
#
# また、負の値はオペレーティング システムとウィンドウマネージャーの
# 判断によるものであることに注意してください。
# 一部のウィンドウマネージャーは、ウィンドウを画面外に配置することを
# 許可しない可能性がある。
#
# 無効な位置はランタイム固有であるが、一般的に位置は
# 最も近い有効な位置にクランプされる。
#
# macOS では、ウィンドウの位置は表示画面領域の左上隅を基準にしている。
# これは、メニューバーが表示されている場合、ウィンドウは
# メニューバーの下に配置されることを意味する。
#
# 注：これは macOS でのみサポートされている。GTK ランタイムはウィンドウの
# 位置設定をサポートしていない。ウィンドウは X11 でのみ自身を配置でき、
# Wayland では配置できないためである。
window-position-x =
window-position-y =
