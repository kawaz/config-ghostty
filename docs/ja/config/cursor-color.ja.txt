# カーソルの色。設定されていない場合はデフォルトの色が選択されます。
#
# 直接色を指定する場合は、16進数（`#RRGGBB` または `RRGGBB`）
# または名前の付いたX11色を使用できます。
#
# さらに、特別な値を使用して色を実行時に
# 他の色に合わせることができます:
#
#   * `cell-foreground` - セルの前景色に合わせます。
#     (利用可能バージョン: 1.2.0以降)
#
#   * `cell-background` - セルの背景色に合わせます。
#     (利用可能バージョン: 1.2.0以降)
cursor-color =
