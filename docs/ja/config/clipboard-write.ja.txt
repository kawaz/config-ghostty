# ターミナルで実行されているプログラムがシステムクリップボードへの
# 読み書きを許可するかどうか（OSC 52、Google で検索）。デフォルトは、
# ユーザーにプロンプトした後にクリップボード読み取りを許可し、
# 無条件で書き込みを許可することである。
#
# 有効な値は：
#
#   * `ask`
#   * `allow`
#   * `deny`
#
clipboard-read = ask
clipboard-write = allow
