# コマンド開始後に変更するディレクトリ。
#
# この設定は `window-inherit-working-directory` 設定の次点に位置します。
# 同じプロセス内に前の Ghostty ターミナルが存在する場合、
# `window-inherit-working-directory` が優先されます。そうでない場合、この
# 設定が使用されます。通常、この設定は最初のウィンドウでのみ使用されます。
#
# デフォルトは `inherit` です。ただし以下に示される特殊なシナリオは除きます。
# macOS では、Ghostty が launchd（ダブルクリック）または `open` から
# 起動されていることを検出できる場合、デフォルトは `home` です。
# GTK を備えた Linux では、Ghostty がデスクトップランチャーから
# 起動されたことを検出できる場合、デフォルトは `home` です。
#
# この値は絶対パスまたは以下の特殊な値のいずれかである必要があります：
#
#   * `home` - 実行ユーザーのホームディレクトリ。
#
#   * `inherit` - 起動プロセスの作業ディレクトリ。
working-directory =
