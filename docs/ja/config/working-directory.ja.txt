# コマンド開始後に変更するディレクトリ。
#
# この設定は `window-inherit-working-directory` 設定の次順です。
# 同じプロセス内に以前の Ghostty ターミナルが存在する場合、
# `window-inherit-working-directory` が優先されます。それ以外の場合、
# この設定が使用されます。通常、この設定は最初のウィンドウにのみ使用されます。
#
# デフォルトは `inherit` です（以下の特別なシナリオを除く）。macOS では、
# Ghostty が launchd（ダブルクリック）または `open` からの起動を検出できた場合、
# デフォルトは `home` になります。Linux（GTK）では、Ghostty がデスクトップ
# ランチャーから起動されたことを検出できた場合、デフォルトは `home` になります。
#
# この値は絶対値であるか、下記の特別な値の 1 つである必要があります：
#
#   * `home` - 実行ユーザーのホームディレクトリ。
#
#   * `inherit` - 起動プロセスの作業ディレクトリ。
working-directory =
