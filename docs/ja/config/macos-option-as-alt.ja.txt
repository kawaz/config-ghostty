# macOSには「alt」キーがなく、代わりに「option」
# キーがあります。オプションキーは少し異なる動作をします。macOSではデフォルトで、
# optionキーと文字を組み合わせると、ときどきUnicode文字が生成されます。
# たとえば、US標準キーレイアウトではoption-bは「∫」を生成します。
# これは、「option」を「alt」キーとして使用したい場合に望ましくないかもしれません。
# ターミナルプログラムやシェルのキーバインディングの場合。
#
# この設定により、optionがaltとして扱われるようにオプションの動作を変更できます。
#
# デフォルト動作（未設定）は、アクティブなキーボード
# レイアウトに依存します。キーボードレイアウトが以下の
# リストされたキーボードレイアウトのいずれかである場合、デフォルト値は「true」です。
# 値。そうでない場合、デフォルト
# 値は「false」です。デフォルト値が「true」のキーボードレイアウトは：
#
#   - U.S. Standard
#   - U.S. International
#
# *Option*-シーケンスが印字可能な文字を生成しない場合、
# この設定に関わらず*Alt*として扱われることに注意してください。（例：`alt+ctrl+a`）。
#
# 設定できる明示的な値：
#
# 「true」の場合、*Option*キーは*Alt*として扱われます。これにより、
# *Alt*を期待するターミナルシーケンスが正常に機能しますが、
# *Alt*キー経由でそれらを使用する場合、macOSのUnicode入力
# シーケンスが破損します。
#
# これを「false」に設定して、macOSの*Alt*キーのunicode
# シーケンスを復元できますが、これは*Alt*を期待する
# ターミナルシーケンスを破損します。
#
# 値「left」または「right」は、左または右の*Option*
# キーに対してこれを有効にします。それぞれ。
macos-option-as-alt =

