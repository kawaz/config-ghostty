# Ghosttyがウィンドウに使用するタイトル。このタイトルが常にウィンドウのタイトル
# として強制され、Ghosttyはプログラム（Neovimなど）が送信するタイトル設定エスケープ
# シーケンスを無視します。
#
# 空白のタイトルが必要な場合は、値を1つ以上のスペースに設定してください。例えば、
# `title = " "` のようにクォートします。これにより事実上タイトルが非表示になります。
# 空白の値を設定するとタイトルがデフォルト値にリセットされるため、これが必要です。
#
# この設定は実行時にリロードできます。設定されている場合、すべてのウィンドウのタイトル
# が更新されます。設定されていない場合、次のタイトル変更エスケープシーケンスは許可されますが、
# 以前の変更は遡及的には設定されません。この後者のケースでは、Neovimなどのプログラムを再起動して
# 新しいタイトルを取得する必要がある場合があります。
title =
