# 背景の不透明度レベル（透明度の反対）。1の値は完全に不透明で、0の値は完全に透明です。0未満または1より大きい値は、最も近い有効な値に固定されます。
#
# macOSでは、ターミナルがネイティブフルスクリーンに入ると、背景の不透明度は無効になります。これは背景がグレーになり、ウィジェットが表示される可能性があり、通常は望ましくないためです。
#
# macOSでは、この設定を変更するとGhosttyを完全に再起動する必要があります。
background-opacity = 1
