# macOSアプリアイコンのフレームに使用するマテリアル。
#
# 有効な値：
#
#  * `aluminum` - ブラッシュされたアルミニウムフレーム。デフォルトです。
#  * `beige` - クラシックな90年代コンピュータベージュフレーム。
#  * `plastic` - 光沢のある暗いプラスチックフレーム。
#  * `chrome` - つやのあるクロームフレーム。
#
# 注記：このオプションは `macos-icon` が `custom-style` に設定されているときに
# 必須です。
macos-icon-frame = aluminum
