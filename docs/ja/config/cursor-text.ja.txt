# カーソルの下にあるテキストの色。これが設定されていない場合、デフォルトが選択されます。
# 16進数（`#RRGGBB` または `RRGGBB`）またはという名前の X11 色として指定されます。
# バージョン 1.2.0 以降、セルの前景色と一致させるために `cell-foreground` に、
# セルの背景色と一致させるために `cell-background` に設定することもできます。
cursor-text =
