# カーソルの下のテキストの色。設定されない場合は、デフォルトが選択されます。
# 16進数（`#RRGGBB` または `RRGGBB`）または X11 名前色として指定されます。
# バージョン 1.2.0 以降、セルの前景色に一致するように `cell-foreground` に、
# またはセルの背景色に一致するように `cell-background` に設定できます。
cursor-text =
