# これが false の場合、cgroup の初期化（linux-cgroup 用）がすべて失敗することが許可され、失敗は無視されます。
# cgroup の分離を「あると便利」な機能として捉え、重要なリソース管理機能ではない場合に有用です。
# cgroup API が失敗した場合でも、Ghostty の起動は失敗しません。
#
# これが true の場合、cgroup の初期化に失敗すると、Ghostty が終了するか新しいサーフェスが作成されません。
#
# 注：現在のところ、これは cgroup の初期化にのみ影響します。サブプロセスは常に
# 独立した cgroup に移動できる必要があります。
linux-cgroup-hard-fail = false
