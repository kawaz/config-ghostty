# ターミナルアプリのUI概念と用語対応

ターミナルエミュレータ、ターミナルマルチプレクサ、UIライブラリにおけるUI概念と用語の整理。

> **最終更新**: 2026-01-18 / **Ghostty**: 1.3.0 (tip channel)

## 階層モデル（L1〜L5）

ターミナルUIは以下の5層で整理できる。ツールによって対応する概念がない層もある。

| 階層 | 概念 | 説明 |
|------|------|------|
| **L1** | 中身 | pty/shell実体。実際のシェルプロセスと接続する部分 |
| **L2** | 分割エリア | 画面を分割したときの1区画 |
| **L2.5** | レイアウトマネージャ | L2の配置パターンを管理・保存する仕組み |
| **L3** | タブ単位 | L2群をまとめるグループ。タブバーに1タブとして表示 |
| **L4** | ウィンドウ/セッション | L3を持つ枠。GUIならOSウィンドウ、仮想端末ならdetach可能な単位 |
| **L5** | プロセス単位 | アプリケーション全体。0個以上のL4を管理 |

### 階層図

```
L5: Server / App
 │
 ├─ L4: Session / Window ─────────────────────┐
 │   │                                         │ タイトルバー
 │   ├─ L3: Window / Tab ───────┐              │ ステータスバー
 │   │   │                      │ タブバー     │
 │   │   ├─ [L2.5: Layout] ─┐   │              │
 │   │   │                  │   │              │
 │   │   ├─ L2: Pane/Split ─┼───┤              │
 │   │   │   └─ L1: Surface │   │              │
 │   │   │                  │   │              │
 │   │   ├─ L2: Pane/Split ─┤   │              │
 │   │   │   └─ L1: Surface │   │              │
 │   │   └──────────────────┴───┘              │
 │   └─────────────────────────────────────────┘
```

## 用語対応表：階層別

各セルは「用語名<br>`検索キーワード`」形式。

| 階層 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| **L1** | Surface<br>`surface` | *(Pane内蔵)* | *(Window内蔵)* | Session | *(Pane内蔵)* |
| **L2** | Split<br>`split` | Pane<br>`pane` | Region<br>`region` | Pane | Pane<br>`pane` |
| **L2.5** | - | Layout<br>`layout` | Layout<br>`layout` | - | - |
| **L3** | Tab<br>`tab` | Window<br>`window` | *(なし)* | Tab | Tab<br>`tab` |
| **L4** | Window<br>`window` | Session<br>`session` | Session<br>`screen` | Window | Window<br>`window` |
| **L5** | *(App)* | Server<br>`server` | *(なし)* | *(App)* | *(App)* |

### 各階層の詳細

#### L1: 中身
- **Ghostty**: `Surface` として明示的に分離（設計上の特徴）
- **tmux/WezTerm**: Paneに内蔵、個別の名前なし
- **iTerm2**: `Session` がこれに相当
- **GNU Screen**: Windowに内蔵

#### L2: 分割エリア
- ほぼ全ツールで存在する概念
- **GNU Screen**の`Region`は特殊：表示領域だけで、閉じてもWindowは死なない

#### L2.5: レイアウトマネージャ
- **tmux**: `Layout`（even-horizontal, main-vertical等のプリセット）
- **GNU Screen**: `Layout`（detach/reattach間でRegion配置を保持）
- GUIターミナルは通常なし（L2を直接操作）

#### L3: タブ単位
- **tmux**では`Window`がこれ（GUIのWindowとは別概念なので注意）
- **GNU Screen**にはこの概念がない（L2.5のLayoutで代用）

#### L4: ウィンドウ/セッション
- **GUIターミナル**: Window（タイトルバー、移動ハンドル付き）
- **tmux/Screen**: Session（detach/attachの単位）

#### L5: プロセス単位
- **tmux**: `Server`（明示的に存在、複数Sessionを管理）
- **GUIアプリ**: 暗黙的（macOSだとマルチウィンドウ対応）
- **GNU Screen**: 明示的な概念なし（各Sessionが独立プロセス的）


## UIコンポーネント対応表

各セルは「用語名<br>`検索キーワード`」形式。キーワードは各ツールの設定やドキュメントを検索する際に使用。

### ウィンドウレベル（L4）

| 概念 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| ウィンドウ枠 | Window Decoration<br>`window-decoration` | - | - | Window Chrome | Window Frame<br>`window_frame` |
| タイトルバー | Title Bar<br>`macos-titlebar` | - | - | Title Bar | Title Bar<br>`window_decorations` |
| タイトル | Window Title<br>`title` | - | - | Window Title | Window Title<br>`window:set_title` |
| ウィンドウボタン | Window Controls | - | - | Traffic Lights | Window Controls |
| プロキシアイコン | Proxy Icon *(macOS)* | - | - | Proxy Icon | - |
| フルスクリーン | Fullscreen<br>`fullscreen`, `toggle_fullscreen` | - | - | Fullscreen | Fullscreen<br>`ToggleFullScreen` |

### タブレベル（L3）

| 概念 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| タブ一覧エリア | Tab Bar<br>`window-show-tab-bar` | Window List<br>`list-windows` | Caption / Hardstatus<br>`caption`, `hardstatus` | Tab Bar | Tab Bar<br>`tab_bar` |
| 個別タブ | Tab<br>`tab`, `new_tab`, `goto_tab` | Window<br>`new-window`, `select-window` | - | Tab | Tab<br>`SpawnTab` |
| タブタイトル | Tab Title<br>`prompt_tab_title` | Window Name<br>`rename-window` | Window Title<br>`title` | Tab Title | Tab Title<br>`tab:set_title` |
| 新規タブボタン | New Tab Button | - | - | + Button | New Tab Button<br>`show_new_tab_button` |
| タブ概要表示 | Tab Overview<br>`toggle_tab_overview` | - | - | Exposé | - |
| タブ移動 | -<br>`previous_tab`, `next_tab`, `move_tab` | -<br>`next-window`, `previous-window` | -<br>`next`, `prev` | - | -<br>`ActivateTab` |

### 分割レベル（L2）

| 概念 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| 分割領域 | Split<br>`split`, `new_split` | Pane<br>`split-window`, `pane` | Region<br>`split`, `region` | Pane | Pane<br>`SplitPane` |
| 境界線 | Split Divider<br>`split-divider-color` | Pane Border<br>`pane-border-style` | - | Divider | Divider |
| リサイズ | Resize<br>`resize_split` | Resize<br>`resize-pane` | Resize<br>`resize` | Resize | Resize<br>`AdjustPaneSize` |
| 分割ヘッダー | Split Header *(hover)* | - | - | Pane Title Bar | - |
| ズーム状態 | Zoomed Split<br>`toggle_split_zoom` | Zoomed Pane<br>`resize-pane -Z` | - | Maximized Pane | Zoomed Pane<br>`TogglePaneZoomState` |
| アクティブ表示 | Focus Indicator<br>`unfocused-split-opacity` | Active Border<br>`pane-active-border-style` | - | Dimming | -<br>`inactive_pane_hsb` |
| 分割移動 | -<br>`goto_split` | -<br>`select-pane` | -<br>`focus` | - | -<br>`ActivatePaneDirection` |
| 均等化 | -<br>`equalize_splits` | -<br>`select-layout tiled` | - | - | - |

### ステータス表示

| 概念 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| 下部ステータス | - | Status Bar<br>`status`, `status-left/right` | Hardstatus<br>`hardstatus` | - | Status Bar<br>`status_bar_*` |
| 上部ステータス | - | -<br>`status-position top` | Caption<br>`caption` | - | Tab Bar<br>`tab_bar_at_bottom` |
| リサイズ表示 | Resize Overlay<br>`resize-overlay` | - | - | - | - |
| モード表示 | - | Mode Indicator<br>`mode-style` | - | - | Mode Indicator |

### 特殊UI

| 概念 | **Ghostty** | **tmux** | **GNU Screen** | **iTerm2** | **WezTerm** |
|------|-------------|----------|----------------|------------|-------------|
| クイックアクセス | Quick Terminal<br>`quick-terminal`, `toggle_quick_terminal` | - | - | Hotkey Window<br>`Hotkey` | - |
| コマンドパレット | Command Palette<br>`toggle_command_palette` | Command Prompt<br>`command-prompt` | - | - | Command Palette<br>`ActivateCommandPalette` |
| 検索バー | Find Bar<br>`search`, `start_search` | Copy Mode<br>`copy-mode` | - | Find Bar | Search<br>`Search` |
| インスペクタ | Inspector<br>`inspector` | - | - | - | Debug Overlay<br>`ShowDebugOverlay` |


## 操作概念と用語対応表

ターミナル操作における概念と各ツールでの用語・検索キーワード・デフォルトキー。
Ghosttyのキーは macOS 表記（⌘=super, ⌥=alt, ⌃=ctrl, ⇧=shift）。

### スクロール操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| スクロールバッファ | 画面外出力の保持 | Scrollback<br>`scrollback-limit` | History<br>`history-limit` | - | Scrollback<br>`scrollback_lines` |
| ページスクロール | 1ページ単位移動 | `scroll_page_up/down`<br>⌘PageUp/Down | `C-b/C-f`<br>(copy-mode) | `C-b`/`C-f` | `ScrollByPage` |
| 行スクロール | 行単位移動 | `scroll_page_lines` | `C-y/C-e`<br>(copy-mode) | `C-y`/`C-e` | `ScrollByLine` |
| 先頭/末尾へ | バッファ端へジャンプ | `scroll_to_top/bottom`<br>⌘Home/End | `g/G`<br>(copy-mode) | `gg`/`G` | `ScrollToTop/Bottom` |
| 選択位置へ | 選択範囲表示 | `scroll_to_selection`<br>⌘J | - | - | - |
| プロンプトジャンプ | 前後のプロンプトへ | `jump_to_prompt`<br>⌘↑/↓, ⌘⇧↑/↓ | - | - | - |

### 選択・コピー操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| 範囲選択 | テキスト選択 | Selection<br>`selection-*` | Selection<br>`copy-mode`, `begin-selection` | Visual Mode<br>`v`, `V`, `C-v` | Selection<br>`SelectText*` |
| 全選択 | 画面全体選択 | `select_all`<br>⌘A | `select-pane -m` | `ggVG` | `SelectAll` |
| 選択調整 | キーボードで拡張 | `adjust_selection`<br>⇧Arrow/PgUp/PgDn/Home/End | `cursor-*`<br>(copy-mode) | Visual拡張 | - |
| コピー | クリップボードへ | `copy_to_clipboard`<br>⌘C | `copy-selection` | `"+y` | `CopyTo` |
| ペースト | 貼り付け | `paste_from_clipboard`<br>⌘V | `paste-buffer` | `"+p` | `PasteFrom` |
| 選択ペースト | X11選択から | `paste_from_selection`<br>⌘⇧V | - | `"*p` | - |
| 選択時コピー | 自動コピー | `copy-on-select` | `copy-selection-and-cancel` | - | `copy_on_select` |
| 矩形選択 | ブロック選択 | - | Rectangle<br>`rectangle-toggle` | `C-v` | `SelectTextBlock` |

### マウス操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| マウスレポート | アプリへ通知 | `mouse-reporting` | `mouse on/off` | `set mouse=a` | `enable_*_mouse` |
| Shift+クリック | 選択拡張 | `mouse-shift-capture` | - | - | - |
| Alt+クリック | カーソル移動 | `cursor-click-to-move` | - | - | - |
| フォーカス追従 | マウス位置で切替 | `focus-follows-mouse` | `focus-events` | `set mouse=a` | `pane_focus_follows_mouse` |

### キーボード設定

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| キーバインド | キー割当 | `keybind` | `bind-key` | `map`, `nmap` | `keys` |
| プレフィックスキー | リーダーキー | `key>key` 構文 | Prefix<br>`prefix` | Leader<br>`mapleader` | Leader<br>`LEADER` |
| キーテーブル | モーダルキー | `activate_key_table` | `bind-key -T` | Mode | `key_tables` |
| キーリマップ | 修飾子入れ替え | `key-remap` | - | - | - |

### 検索操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| 検索開始 | 検索モード | `start_search`<br>⌘F | `copy-mode /` | `/`, `?` | `Search` |
| 検索終了 | 検索解除 | `end_search`<br>Esc, ⌘⇧F | - | - | - |
| 次/前の一致 | 結果移動 | `navigate_search`<br>⌘G / ⌘⇧G | `n/N`<br>(copy-mode) | `n`/`N` | `CycleMatch*` |
| 選択で検索 | 選択文字列検索 | `search_selection`<br>⌘E | - | `*`, `#` | - |

### カーソル表示

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| カーソル形状 | block/bar/underline | `cursor-style` | `cursor-style` | `guicursor` | `default_cursor_style` |
| カーソル点滅 | 点滅有無 | `cursor-style-blink` | - | `guicursor` (blink) | `cursor_blink_rate` |
| カーソル色 | 色指定 | `cursor-color` | `cursor-colour` | `guicursor` | `cursor_*` |

### フォント操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| フォント指定 | フォント設定 | `font-family`, `font-size` | - | `guifont` | `font`, `font_size` |
| サイズ拡大 | 動的変更 | `increase_font_size`<br>⌘+, ⌘= | - | - | `IncreaseFontSize` |
| サイズ縮小 | 動的変更 | `decrease_font_size`<br>⌘- | - | - | `DecreaseFontSize` |
| サイズリセット | 初期値に戻す | `reset_font_size`<br>⌘0 | - | - | `ResetFontSize` |

### 画面操作

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| 画面クリア | クリア | `clear_screen`<br>⌘K | `clear-history` | `:!clear` | `ClearScrollback` |
| リセット | 状態初期化 | `reset` | - | `:reset` | - |
| 画面保存 | ファイル出力 | `write_screen_file`<br>⌘⇧J (paste) | `capture-pane` | - | - |
| スクロールバック保存 | 履歴出力 | `write_scrollback_file` | `save-buffer` | - | - |

### ウィンドウ・タブ・分割

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| 新規ウィンドウ | ウィンドウ作成 | `new_window`<br>⌘N | `new-session` | `:new` | `SpawnWindow` |
| 新規タブ | タブ作成 | `new_tab`<br>⌘T | `new-window` | `:tabnew` | `SpawnTab` |
| タブ移動 | 前後タブへ | `previous/next_tab`<br>⌃Tab, ⌘⇧[/] | `previous/next-window` | `gT`/`gt` | `ActivateTab*` |
| タブ指定 | 番号で移動 | `goto_tab`<br>⌘1〜9 | `select-window -t` | `:tabn` | `ActivateTab` |
| 新規分割 | 分割作成 | `new_split`<br>⌘D (右), ⌘⇧D (下) | `split-window` | `:sp`, `:vs` | `SplitPane` |
| 分割移動 | フォーカス移動 | `goto_split`<br>⌘[/], ⌘⌥Arrow | `select-pane` | `C-w hjkl` | `ActivatePaneDirection` |
| 分割リサイズ | サイズ変更 | `resize_split`<br>⌘⌃Arrow | `resize-pane` | `C-w <>+-` | `AdjustPaneSize` |
| 分割ズーム | 最大化 | `toggle_split_zoom`<br>⌘⇧Enter | `resize-pane -Z` | - | `TogglePaneZoomState` |
| 分割均等化 | 均等配置 | `equalize_splits`<br>⌘⌃= | `select-layout tiled` | `C-w =` | - |
| 閉じる | 閉じる | `close_surface`<br>⌘W | `kill-pane` | `:q` | `CloseCurrentPane` |
| フルスクリーン | 全画面 | `toggle_fullscreen`<br>⌘Enter, ⌘⌃F | - | - | `ToggleFullScreen` |

### 設定・その他

| 概念 | 説明 | **Ghostty** | **tmux** | **Vim** | **WezTerm** |
|------|------|-------------|----------|---------|-------------|
| 設定リロード | 再読み込み | `reload_config`<br>⌘⇧, | `source-file` | `:source` | `ReloadConfiguration` |
| 設定ファイル | 設定を開く | `open_config`<br>⌘, | - | `:e $MYVIMRC` | - |
| コマンドパレット | コマンド一覧 | `toggle_command_palette`<br>⌘⇧P | `command-prompt` | - | `ActivateCommandPalette` |
| インスペクタ | デバッグ | `inspector`<br>⌘⌥I | - | - | `ShowDebugOverlay` |
| 終了 | アプリ終了 | `quit`<br>⌘Q | `kill-server` | `:qa` | `QuitApplication` |
| Undo | 操作取消 | `undo`<br>⌘Z, ⌘⇧T | - | `u` | - |
| Redo | やり直し | `redo`<br>⌘⇧Z | - | `C-r` | - |
| 読み取り専用 | 入力禁止 | `toggle_readonly` | - | `:set readonly` | - |


## キーバインド記述方法の比較

各ツールでのキーバインド表記法の違いと対応。

### 修飾キー名の対応

| 物理キー (macOS) | 物理キー (他OS) | **Ghostty** | **tmux** | **Vim** | **Emacs** | **WezTerm** |
|-----------------|----------------|-------------|----------|---------|-----------|-------------|
| Command (⌘) | Super/Win | `super`, `cmd`, `command` | - | `<D->` | `s-` | `SUPER`, `CMD` |
| Option (⌥) | Alt | `alt`, `opt`, `option` | `M-` | `<A->`, `<M->` | `M-` (Meta) | `ALT`, `OPT` |
| Control (⌃) | Ctrl | `ctrl`, `control` | `C-` | `<C->` | `C-` | `CTRL` |
| Shift (⇧) | Shift | `shift` | `S-` | `<S->` | `S-` | `SHIFT` |
| Fn | Fn | *(非対応)* | - | - | - | - |

### キー表記法の比較

| 入力 | **Ghostty** | **tmux** | **Vim** | **Emacs** | **端末表記** | **ASCII値** |
|------|-------------|----------|---------|-----------|-------------|-------------|
| Ctrl+A | `ctrl+a` | `C-a` | `<C-a>` | `C-a` | `^A` | 0x01 |
| Ctrl+[ | `ctrl+[` | `C-[` | `<C-[>` | `C-[` | `^[` (ESC) | 0x1B |
| Alt+A | `alt+a` | `M-a` | `<A-a>` | `M-a` | `ESC a` or `\ea` | - |
| Cmd+A (macOS) | `super+a` | - | `<D-a>` | `s-a` | - | - |
| Shift+A | `shift+a` or `A` | `S-a` | `<S-a>` or `A` | `S-a` or `A` | `A` | 0x41 |
| Ctrl+Shift+A | `ctrl+shift+a` | `C-S-a` | `<C-S-a>` | `C-S-a` | - | - |

### 特殊キーの対応

| キー | **Ghostty** | **tmux** | **Vim** | **Emacs** | **端末シーケンス** |
|------|-------------|----------|---------|-----------|-------------------|
| Escape | `escape` | `Escape` | `<Esc>` | `ESC` | `^[` / `\e` / `\x1b` |
| Enter | `enter` | `Enter` | `<CR>`, `<Enter>` | `RET` | `^M` / `\r` / `\x0d` |
| Tab | `tab` | `Tab` | `<Tab>` | `TAB` | `^I` / `\t` / `\x09` |
| Backspace | `backspace` | `BSpace` | `<BS>` | `DEL` | `^?` / `\x7f` or `^H` |
| Delete | `delete` | `DC` | `<Del>` | `<delete>` | CSI 3~ |
| Space | `space` | `Space` | `<Space>` | `SPC` | ` ` / `\x20` |
| 上矢印 | `up`, `arrow_up` | `Up` | `<Up>` | `<up>` | CSI A |
| 下矢印 | `down`, `arrow_down` | `Down` | `<Down>` | `<down>` | CSI B |
| 左矢印 | `left`, `arrow_left` | `Left` | `<Left>` | `<left>` | CSI D |
| 右矢印 | `right`, `arrow_right` | `Right` | `<Right>` | `<right>` | CSI C |
| Home | `home` | `Home` | `<Home>` | `<home>` | CSI H or CSI 1~ |
| End | `end` | `End` | `<End>` | `<end>` | CSI F or CSI 4~ |
| Page Up | `page_up` | `PPage` | `<PageUp>` | `<prior>` | CSI 5~ |
| Page Down | `page_down` | `NPage` | `<PageDown>` | `<next>` | CSI 6~ |
| Insert | `insert` | `IC` | `<Insert>` | `<insert>` | CSI 2~ |
| F1〜F12 | `f1`〜`f12` | `F1`〜`F12` | `<F1>`〜`<F12>` | `<f1>`〜`<f12>` | CSI 11~〜CSI 24~ |

### キーシーケンス（連続入力）の表記

| 概念 | **Ghostty** | **tmux** | **Vim** | **Emacs** |
|------|-------------|----------|---------|-----------|
| 基本構文 | `key1>key2` | `key1 key2` (bind-key -T) | `key1key2` (map) | `key1 key2` (define-key) |
| 例: prefix + n | `ctrl+a>n` | `C-a n` (prefix table) | - | `C-x n` |
| リーダーキー | `>` で連結 | prefix + bind-key | `<Leader>` | prefix-map |

**Ghosttyの例:**
```ini
keybind = ctrl+a>n=new_window
keybind = ctrl+a>ctrl+a=text:\x01  # Ctrl+A自体を送信
```

**tmuxの例:**
```
bind-key -T prefix n new-window
bind-key -T prefix C-a send-prefix
```

### エスケープシーケンスとキー入力の対応

端末アプリケーションでは、特定のキー入力がエスケープシーケンスとして送信される。

| 表記 | 意味 | キー入力 | Ghosttyでの送信方法 |
|------|------|----------|-------------------|
| `^[` | ESC (0x1B) | Ctrl+[ または Escape | `esc:` アクション |
| `^A`〜`^Z` | Ctrl+A〜Z (0x01〜0x1A) | Ctrl+該当キー | `text:\x01`〜`text:\x1a` |
| `^?` | DEL (0x7F) | Backspace (多くの端末) | `text:\x7f` |
| `CSI` | Control Sequence Introducer | - | `csi:` アクション |
| `ESC [` | CSI (0x1B 0x5B) | - | `esc:[` or `csi:` |
| `ESC O` | SS3 (0x1B 0x4F) | - | `esc:O` |

**Ghosttyでの送信例:**
```ini
# Ctrl+Uと同じ効果（行頭まで削除）
keybind = super+backspace=text:\x15

# カーソル上移動（CSI A）
keybind = custom_key=csi:A

# Alt+D相当（単語削除）
keybind = super+d=esc:d
```

### 物理キーコード vs 論理キー

Ghosttyは物理キーコード（W3C準拠）と論理キー（文字）の両方をサポート。

| 概念 | **Ghostty表記** | 説明 |
|------|----------------|------|
| 論理キー | `a`, `1`, `-` | キーボードレイアウトに依存 |
| 物理キーコード | `KeyA`, `Digit1`, `Minus` | レイアウト非依存（US配列基準） |
| スネークケース | `key_a`, `digit_1` | 物理キーの別表記 |

```ini
# 論理キー: AZERTY配列では Q キーで発火
keybind = ctrl+a=select_all

# 物理キー: どの配列でも同じ物理位置で発火
keybind = ctrl+KeyA=select_all
```

### Ghosttyキーバインドのプレフィックス

| プレフィックス | 意味 | 例 |
|--------------|------|-----|
| `global:` | アプリ非フォーカス時も有効 | `global:super+space=toggle_quick_terminal` |
| `all:` | 全ターミナルサーフェスに適用 | `all:super+k=clear_screen` |
| `unconsumed:` | キー入力をアプリにも送信 | `unconsumed:ctrl+a=reload_config` |
| `performable:` | 実行可能な時のみ消費 | `performable:super+c=copy_to_clipboard` |

複数プレフィックス組み合わせ: `global:unconsumed:ctrl+a=reload_config`


## Unicode文字幅と Ambiguous Width 問題

ターミナルにおける文字幅問題の詳細な解説。

### 問題の概要

Unicode の East Asian Width 特性において、一部の文字は **Ambiguous（曖昧）** に分類される。
これらの文字（罫線文字、ギリシャ文字、一部の記号など）は、コンテキストによって幅1（半角）または幅2（全角）のいずれかで表示される。

**問題の本質**: 出力側（TUIアプリ）と表示側（ターミナル）が異なる幅を想定すると表示が崩れる。

```
出力アプリが想定する幅  ≠  ターミナルが表示する幅  →  カーソル位置ズレ、罫線崩れ
```

### なぜ厄介か

1. **地域による慣習の違い**
   - 英語圏: Ambiguous文字 = 幅1（半角）がデフォルト
   - CJK圏（日中韓）: Ambiguous文字 = 幅2（全角）が伝統的

2. **2つのアプリの組み合わせ問題**
   - TUIアプリ（vim, tmux, シェルなど）が文字幅をいくつとして**出力**するか
   - ターミナル（Ghostty, iTerm2など）が文字幅をいくつとして**表示**するか
   - 両者が一致しないと表示が崩れる

3. **一律の正解がない**
   - Unicode標準は「コンテキスト依存」としており、固定値を定めていない
   - アプリごとに設定が必要で、統一が難しい

4. **「どの文字がAmbiguousか」の定義自体にズレがある**（後述）


### Unicode East Asian Width の定義

[UAX #11 (Unicode Standard Annex #11)](https://www.unicode.org/reports/tr11/) で各コードポイントに以下のプロパティが割り当てられている。

| プロパティ | 略称 | 幅 | 説明 | 例 |
|-----------|------|-----|------|-----|
| **Fullwidth** | F | 2 | 全角互換文字 | `Ａ` (U+FF21) |
| **Halfwidth** | H | 1 | 半角互換文字 | `ｱ` (U+FF71) |
| **Wide** | W | 2 | CJK文字等 | `漢` `あ` `ア` |
| **Narrow** | Na | 1 | ASCII等 | `A` `a` `1` |
| **Ambiguous** | A | **1 or 2** | コンテキスト依存 | `→` `α` `○` |
| **Neutral** | N | 1 | その他 | 多くの記号 |

**公式データソース**: [EastAsianWidth.txt](https://www.unicode.org/Public/UCD/latest/ucd/EastAsianWidth.txt)

```
# EastAsianWidth.txt の例（Unicode 15.1）
0391..03A1;A     # Α〜Ρ ギリシャ大文字 → Ambiguous
2190..2199;A     # ← → ↑ ↓ 等の矢印 → Ambiguous
2500..257F;A     # Box Drawing 罫線 → Ambiguous
25A0..25FF;A     # Geometric Shapes → Ambiguous
3000;F           # 　（全角スペース）→ Fullwidth
4E00..9FFF;W     # CJK統合漢字 → Wide
```

### Ambiguous Width に該当する文字の例

| 文字 | 名称 | Unicode | East Asian Width | 英語圏での扱い | CJK圏での扱い |
|------|------|---------|------------------|---------------|--------------|
| → | 矢印 | U+2192 | Ambiguous | 幅1 | 幅2 |
| α | ギリシャ小文字 | U+03B1 | Ambiguous | 幅1 | 幅2 |
| Σ | ギリシャ大文字 | U+03A3 | Ambiguous | 幅1 | 幅2 |
| № | ナンバーサイン | U+2116 | Ambiguous | 幅1 | 幅2 |
| ─ | 罫線（横） | U+2500 | Ambiguous | 幅1 | 幅1 or 2 |
| │ | 罫線（縦） | U+2502 | Ambiguous | 幅1 | 幅1 or 2 |
| ■ | 黒四角 | U+25A0 | Ambiguous | 幅1 | 幅2 |
| ○ | 白丸 | U+25CB | Ambiguous | 幅1 | 幅2 |
| ※ | 米印 | U+203B | Ambiguous | 幅1 | 幅2 |
| ℃ | 摂氏 | U+2103 | Ambiguous | 幅1 | 幅2 |


### 実装ごとの「Ambiguous文字リスト」のズレ

**重要**: Unicodeで定義は統一されているが、**実装ごとに参照するデータが異なる**。

```
┌─────────────────────────────────────────────────────────────┐
│ ズレが生じる原因                                             │
├─────────────────────────────────────────────────────────────┤
│ 1. Unicodeバージョンの違い                                   │
│    - Unicode 15.1 (2023) vs 14.0 (2021) vs 13.0 (2020)      │
│    - 新しいバージョンで分類が変更された文字がある             │
│    - 絵文字は特に頻繁に追加・変更される                       │
├─────────────────────────────────────────────────────────────┤
│ 2. wcwidth() 実装の違い                                      │
│    - glibc (Linux): glibcバージョンに依存                    │
│    - musl libc: 独自実装                                     │
│    - macOS libc: Apple独自                                   │
│    - 各OSのlibcが参照するUnicodeバージョンが異なる            │
├─────────────────────────────────────────────────────────────┤
│ 3. アプリ独自の幅計算テーブル                                 │
│    - libc wcwidth() を使わず独自テーブルを持つアプリがある    │
│    - 同じ文字でもアプリによって幅判定が異なる場合がある        │
├─────────────────────────────────────────────────────────────┤
│ 4. 「Ambiguousをどう扱うか」の違い                           │
│    - 全Ambiguous文字を幅2にする                              │
│    - 一部のAmbiguous文字だけ幅2にする                        │
│    - 罫線(Box Drawing)は幅1のまま、記号だけ幅2、など          │
└─────────────────────────────────────────────────────────────┘
```

#### 各言語・ライブラリの文字幅実装

| 言語/環境 | ライブラリ/関数 | 特徴 |
|----------|----------------|------|
| **C (glibc)** | `wcwidth()` | ロケール依存、glibcバージョンでUnicodeデータが異なる |
| **C (musl)** | `wcwidth()` | 独自実装、glibcと結果が異なる場合あり |
| **C (macOS)** | `wcwidth()` | Apple独自、比較的新しいUnicodeに追従 |
| **Go** | `go-runewidth` | 独自テーブル、EastAsianWidth対応オプションあり |
| **Rust** | `unicode-width` | Unicode標準準拠、定期的に更新 |
| **Python** | `wcwidth` パッケージ | Unicode標準準拠、Pythonバージョンで異なる場合あり |
| **Node.js** | `wcwidth` パッケージ | 独自実装 |
| **Vim** | `strwidth()` | 独自実装 + `ambiwidth` オプション |

#### 具体例: 同じ文字の異なる判定

```
U+2503 (┃ 太い縦罫線) の扱い:

┌──────────────────┬────────────────────────────────┐
│ 環境             │ 幅判定                          │
├──────────────────┼────────────────────────────────┤
│ Unicode UAX#11   │ Ambiguous (A)                  │
│ glibc 2.31       │ 1                              │
│ glibc 2.35       │ 1                              │
│ macOS 14         │ 1                              │
│ go-runewidth     │ 1 (EastAsian=false)            │
│ go-runewidth     │ 2 (EastAsian=true)             │
│ Vim ambiwidth=s  │ 1                              │
│ Vim ambiwidth=d  │ 2                              │
└──────────────────┴────────────────────────────────┘
```

### 各ツールの Ambiguous Width 設定

| ツール | 設定 | 値 | 説明 |
|--------|------|-----|------|
| **Ghostty** | - | - | **Ambiguous Width専用設定は現在なし** |
| **WezTerm** | `treat_east_asian_ambiguous_width_as_wide` | `true` / `false` | Ambiguous を幅2として扱う |
| **iTerm2** | Preferences → Profiles → Text | checkbox | "Ambiguous characters are double-width" |
| **Alacritty** | - | - | 設定なし（常にUnicode標準） |
| **kitty** | - | - | 設定なし（常にUnicode標準） |
| **libvte** | - | - | ロケール依存（`LC_CTYPE`） |
| **Vim/Neovim** | `ambiwidth` | `single` / `double` | Ambiguous文字の幅を指定 |
| **Emacs** | `east-asian-ambiguous-width` | custom | `set-char-table-range` で個別設定 |
| **tmux** | *(削除済)* | - | 2.5以前は`utf8-cjk`があったが削除。現在はwcwidth依存 |
| **zsh** | - | - | wcwidth依存。PROMPTでは`%{..%}`で幅ヒント |
| **bash** | - | - | readline経由でwcwidth依存 |

### Ghostty の現状

Ghostty 1.3.0 時点では、Ambiguous Width を明示的に制御する設定は**存在しない**。

関連する設定:
- `grapheme-width-method`: grapheme cluster（絵文字の肌色修飾子など）の幅計算方式
  - `unicode`: Unicode標準に従う（デフォルト）
  - `legacy`: `wcswidth` 等のレガシー方式
- Terminal Mode 2027: アプリがUnicode幅を強制できる

**注意**: `grapheme-width-method` は Ambiguous Width とは**別の問題**を扱う。

| 問題 | 対象 | 例 | Ghostty設定 |
|------|------|-----|-------------|
| **Grapheme Width** | grapheme cluster | 👨‍👩‍👧 (家族絵文字) | `grapheme-width-method` |
| **Ambiguous Width** | East Asian Width=A の文字 | → α ○ | **設定なし** |

### font-codepoint-map では解決できない

Ghosttyの `font-codepoint-map` は特定のコードポイントに別フォントを割り当てる機能だが、
**Ambiguous Width問題の解決には使えない**。

```
ターミナルの描画プロセス:

1. セル幅計算（コードポイント → wcwidth等）
   U+2192 (→) → 「1セル」と判定  ← ここで幅が決まる
                                   フォントは関与しない

2. グリフ取得（font-codepoint-map 適用）
   指定フォントからグリフを取得

3. 描画
   「1セル分の幅」にグリフを収めて描画
   （グリフ自体のデザイン幅は無関係）
```

つまり:
- **セル幅はコードポイントから計算され、フォントは関与しない**
- 幅2のグリフを持つフォントを指定しても、1セルに押し込まれる
- フォント変更で見た目は変わるが、カーソル位置ズレは解消しない

### 「文字+スペース」置換による回避も不可

「ターミナル側でAmbiguous文字を『文字+半角スペース』に内部置換すれば2幅になるのでは？」
→ **これも解決にならない**

```
┌────────────────────────────────────────────────────────┐
│ TUIアプリ (vim等)                                       │
│   「→」を1幅として計算し、カーソル位置を決定            │
│   出力: "A→B" → Aが1列目、→が2列目、Bが3列目と想定     │
└────────────────────────────────────────────────────────┘
                    ↓ 出力
┌────────────────────────────────────────────────────────┐
│ ターミナル（仮に置換機能があったとして）                 │
│   「→」を「→ 」(+スペース)に置換して表示               │
│   表示: "A→ B" → Bは実際には4列目に表示される          │
└────────────────────────────────────────────────────────┘
                    ↓ 結果
┌────────────────────────────────────────────────────────┐
│ カーソル位置ズレ発生                                    │
│   アプリ: 「Bは3列目にある」と認識                      │
│   実際:   「Bは4列目に表示されている」                  │
│   → カーソルがBの1つ左を指すことになる                 │
└────────────────────────────────────────────────────────┘
```

**根本原則**: 出力アプリとターミナルの**両方**が同じ幅を想定しないと解決しない。


### 推奨される対処法

#### 1. 出力アプリ側で設定を揃える（最も確実）

```vim
" Vim/Neovim: Ghosttyのデフォルト（幅1）に合わせる
set ambiwidth=single

" CJK環境で幅2が必要な場合（ターミナル側も対応している前提）
set ambiwidth=double
```

```lua
-- Neovim (Lua)
vim.opt.ambiwidth = 'single'
```

#### 2. ロケール設定を確認

```bash
# 現在のロケール確認
locale

# UTF-8ロケールであることを確認
# LANG=en_US.UTF-8 や LANG=ja_JP.UTF-8 など

# 一部ツールはLC_CTYPEがCJKロケールの場合にAmbiguous=2と解釈
```

#### 3. 問題が起きる文字を避ける

```bash
# 罫線を使うコマンドでASCIIモードを指定
tree --charset=ASCII
ls --ignore-backups  # 代わりにシンプルな出力

# gitのグラフ表示
git log --graph --oneline --ascii
```

#### 4. シェルプロンプトでの対処

```bash
# zsh: %{..%} で「この中は幅0」とシェルに伝える
# 幅計算に含めたくない制御文字やアイコンを囲む
PROMPT='%{→%} %~ $ '  # →の幅を0として扱わせる（表示はされる）

# 自分で明示的にスペースを追加して調整
PROMPT='→  %~ $ '  # 見た目を2幅分にする（力技）
```

#### 5. Ghosttyへの機能要望

将来的に以下のような設定が追加されることが望ましい:

```ini
# 希望する設定（現在は存在しない）
ambiguous-width = double  # または single
# もしくは
east-asian-width-ambiguous = wide  # または narrow
```

他ターミナルの実装例:
- **WezTerm**: `treat_east_asian_ambiguous_width_as_wide = true`
- **iTerm2**: Preferences → Profiles → Text → "Ambiguous characters are double-width"

要望先: [Ghostty GitHub Issues](https://github.com/ghostty-org/ghostty/issues)


### 参考リンク

- [UAX #11: East Asian Width](https://www.unicode.org/reports/tr11/)
- [EastAsianWidth.txt (Unicode公式データ)](https://www.unicode.org/Public/UCD/latest/ucd/EastAsianWidth.txt)
- [wcwidth(3) man page](https://man7.org/linux/man-pages/man3/wcwidth.3.html)
- [go-runewidth](https://github.com/mattn/go-runewidth) - Go言語の文字幅ライブラリ
- [unicode-width (Rust)](https://crates.io/crates/unicode-width) - Rustの文字幅ライブラリ
- [wcwidth (Python)](https://pypi.org/project/wcwidth/) - Pythonの文字幅ライブラリ


## UIライブラリ用語対応表

### 分割コンテナ

| 概念 | **GTK** | **Qt** | **SwiftUI/AppKit** | **Web/CSS** |
|------|---------|--------|-------------------|-------------|
| 分割コンテナ | GtkPaned | QSplitter | NSSplitView / HSplitView | flex container |
| 子要素 | Child | Widget | View | flex item |
| 分割方向 | Orientation | Orientation | - | flex-direction |

### 境界線・ハンドル

| 概念 | **GTK** | **Qt** | **SwiftUI/AppKit** | **Web/CSS** |
|------|---------|--------|-------------------|-------------|
| 境界線 | Separator | - | Divider | border |
| ドラッグ領域 | Handle | Splitter Handle | *(Divider内蔵)* | resize handle |
| 境界線スペース | Gutter | Handle Width | - | gap |
| 分割位置 | Position | Position | - | flex-basis |

### GTKの用語補足

GTKは文脈によって複数の用語を使い分ける：

- **Separator**: CSSノード名（`paned > separator`）
- **Handle**: ユーザーがドラッグする部分の機能名
- **Divider**: 位置を説明するときの一般名
- **Gutter**: 境界線が存在するスペース
- **Slider**: 古いドキュメントで見られる呼び方


## Ghostty固有の概念

### Surface
Ghosttyの設計上の特徴として、UIコンテナ（Window/Tab/Split）とターミナル実体（Surface）を明確に分離している。

```
┌─────────────────────────────────────┐
│  Core (Surface)                     │ ← ターミナルエミュレーション本体
│  - pty管理、エスケープシーケンス     │    プラットフォーム非依存
├─────────────────────────────────────┤
│  AppRT (Application Runtime)        │ ← 橋渡し層
├─────────────────────────────────────┤
│  Platform UI (Window/Tab/Split)     │ ← OS固有のネイティブUI
│  - macOS: AppKit/SwiftUI            │
│  - Linux: GTK                       │
└─────────────────────────────────────┘
```

### Split Header（分割ヘッダー）
マウスカーソルをSplit上部に持っていくと表示される動的なUI要素。Splitの移動やタイトル表示に使用。

### Quick Terminal
macOSでメニューバー下からアニメーションで出現する軽量ターミナル。Spotlightのような使い方が可能。


## 参考リンク

- [Ghostty Features](https://ghostty.org/docs/features)
- [Ghostty Config Reference](https://ghostty.org/docs/config/reference)
- [tmux Getting Started](https://github.com/tmux/tmux/wiki/Getting-Started)
- [GNU Screen Manual](https://www.gnu.org/software/screen/manual/screen.html)
- [WezTerm Appearance](https://wezterm.org/config/appearance.html)
- [GTK GtkPaned](https://docs.gtk.org/gtk3/class.Paned.html)
